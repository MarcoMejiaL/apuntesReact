const prices:(number|string)[] = [1,2,3,4,5,'hola']; //<=no importa la posicion ni cantidad, solo que contenga los elementos definidos


let user :[string,number]; //<= se define una tupla desde el principio del array y se maneja en orden y cantidad
user =['marco',32]
